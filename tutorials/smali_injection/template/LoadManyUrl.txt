#method. Suggested to paste it at the end of the code.



#caller. Paste it at first line of the onCreate() method
invoke-direct {p0}, Lchange/this_path_to_the_path_of_the_class_that_you_plan_to_modify/MainActivity;->iq500LoadManyUrl()V
#You have to manually change the current activity (Lchange...)

#Java logic is as follows:
	private void iq500LoadManyUrl()
	{
		String list = "pornhub.com,thisav.com,redtube.com";
		String[] list2 = list.split(",");
		
		for(int i = 0; i < list2.length; i++)
		{
			Intent browserIntent = new Intent(Intent.ACTION_VIEW, Uri.parse("http://" + list2[i]));
			startActivity(browserIntent);
		}
		
	}
